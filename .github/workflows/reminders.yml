name: MyLife reminders
on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the reminder sender
        run: curl -fsS "https://mylife-notes.vercel.app/api/send-reminders?key=wingreminders"
